//DEFINE ONLY THE OBJECT
//MODULE DEFINTION IS RETURNING OBJECT

//Inside file my/shirt.js:
define({
    color: "black",
    size: "unisize"
});

//Only the first define is taken

define({
    color: "black",
    size: "unisize2"
});